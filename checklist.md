# Self‑Check (Single Page)

## Core concepts (tick when you can *explain + implement*)
- [ ] Linear regression: normal equation vs. gradient descent; feature scaling rationale.
- [ ] Logistic regression: sigmoid, cross‑entropy; L2 regularization effect.
- [ ] Bias–variance: learning curves; under/over‑fitting signals.
- [ ] Model selection: train/val/test; k‑fold; data leakage pitfalls.
- [ ] Trees & ensembles: split criteria (Gini/Entropy), max_depth; bagging vs boosting (high‑level).
- [ ] Basic neural network: forward/backprop for a 2‑layer MLP; activation choice.
- [ ] Unsupervised: k‑means objective; PCA variance explained (high‑level).
- [ ] Recommenders: user‑user CF vs. matrix factorization (intuition only).
- [ ] RL intro: multi‑armed bandits; ε‑greedy vs UCB (intuition + small code).

## Coding minimums (tick when done)
- [ ] Linear regression (GD) from scratch on a toy dataset (RMSE < baseline).
- [ ] Logistic regression from scratch; AUC/accuracy ≥ baseline.
- [ ] k‑fold CV + learning curves.
- [ ] DecisionTreeClassifier + hyperparam tuning.
- [ ] k‑means (elbow/silhouette) + PCA EVR plot.
- [ ] User‑user CF top‑N on toy ratings.
- [ ] Bandit ε‑greedy + UCB simulation with regret plot.
